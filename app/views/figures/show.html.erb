<% if current_user.admin? %>
  <div class="row">
    <div class="col-md-4">
      <section class="user_info">
        <h1>
          <%= @figure.name %>
        </h1>
        <button id="sendMessageButton" type="submit"><%= link_to "edit", edit_figure_path(@figure) %></button>
        <button id="sendMessageButton" type="submit"><%= link_to "delete",figure_path(@figure), method: :delete %></button>
      </section>
    </div>
  </div>
<% end %>
