<%= form_for( current_user.active_favourites.build, remote: true ) do |f| %>
  <div><%= hidden_field_tag :favourited_id, @tale.id %></div>
  <%= f.submit "Yêu thích", class: "btn btn-primary" %>
<% end %>
