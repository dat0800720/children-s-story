<%= form_for(current_user.active_favourites.find_by(favourited_id: @tale.id),
    html: { method: :delete }, remote: true) do |f| %>
    <div><%= hidden_field_tag :favourited_id, @tale.id %></div>
  <%= f.submit "Bỏ Yêu thích", class: "btn btn-primary" %>
<% end %>
