<div class="container">
  <div class="contact">
    <div class="title_request">
      <h2>Người gửi: <%= @request.user.name %></h2>
    </div>
    <div class="title_request">
      <p><strong>tiêu đề: <%= @request.title %></strong></p>
    </div>
    <div class="title_request">
      <p style="word-wrap: break-word;">Nội dung: <%= @request.content %></p>
    </div>
    <div class="time">
      <%= @request.created_at&.strftime("%H : %M Ngày %d/%m/%Y") %>
    </div>
  </div>
</div>
