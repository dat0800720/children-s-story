<h2><%= reviewtale.user.name %></h2>
<div class="review-rating" data-score=<%= reviewtale.rating %>></div>
<p><%= reviewtale.comment%></p>

<% if logged_in? %>
  <% if reviewtale.user_id == current_user.id%>
    <button id="sendMessageButton" type="submit"><%= link_to "sửa",  edit_tale_reviewtale_path(tale_id: reviewtale.tale.id,id: reviewtale.id) %></button>
    <button id="sendMessageButton" type="submit"><%= link_to "xóa", tale_reviewtale_path(tale_id: reviewtale.tale.id,id: reviewtale.id), method: :delete %></button>
  <% end %>
<% end %>
