<% @user ||= current_user %>
<div class="stats">
  <span class="posted_in">
    <strong>Lượt theo dõi:</strong> 
    <strong id="followers" class="stat"><%= @tale.followers.count %></strong>
  </span>
</div>
<div class="stats">
  <span class="posted_in">
    <strong>Lượt yêu thích:</strong> 
    <strong id="favouriters" class="stat"><%= @tale.favouriters.count %></strong>
  </span>
</div>
